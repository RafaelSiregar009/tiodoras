<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- FIX: izin ini WAJIB ada supaya aplikasi bisa akses internet sama
         sekali (login, Supabase, dst). Tanpa ini, Android blokir semua
         koneksi jaringan aplikasi di level sistem SEBELUM sempat mencoba
         konek — makanya errornya selalu "Failed host lookup" / DNS gagal
         persis sama terus, walau jaringan/DNS/VPN di HP-nya sendiri normal. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- FIX (fitur baru): wajib di Android 13+ (API 33) supaya aplikasi
         boleh menampilkan notifikasi sama sekali — tanpa ini, push
         notification dari FCM diam-diam tidak pernah muncul di status bar,
         tanpa error apa pun (dan izin ini juga yang diminta lewat dialog
         permission saat admin login, lihat fcm_service.dart). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="Tiodoras V3"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FIX (fitur baru): channel default untuk notifikasi push FCM
             (lihat "channel_id" yang sama di
             supabase/functions/notify-admin-pengajuan/index.ts) & ikon
             default-nya, supaya notifikasi tetap bisa muncul walau
             aplikasi sedang di-background/tertutup total (bukan cuma saat
             foreground). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="pengajuan_channel" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/launcher_icon" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
